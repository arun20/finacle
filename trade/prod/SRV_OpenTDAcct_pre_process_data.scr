<--START
TRACE ON
	IF (FIELDEXISTS(BANCS.STDIN.menuId)) THEN
	#{
		IF (BANCS.STDIN.menuId == "HOAACTD") THEN
		#{
			CALLSCRIPTIFEXIST("tdOpenTranRestrict.scr")
		#}
		ENDIF
	#}
	ENDIF
TRACE OFF
EXITSCRIPT
END-->
